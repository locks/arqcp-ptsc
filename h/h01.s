; Enunciado:
;
; H1:	Desenvolva um esquema de dados capaz de armazenar a seguinte informação referente a um funcionário:
;	- número (4 bytes);
;	- nome (max. 50 bytes);
;	- vencimento (4 bytes);
;	- categoria (0-15);
;	- chefe (s/n);
;	- activo (s/n).
;
;	Apresente um exemplo.
; 	Como se poderá armazenar informação de vários funcionários?
;

.file "ex_h_1.s"
